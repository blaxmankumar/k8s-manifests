apiVersion: v1
kind: Service
metadata:
  name: express-backend-service
  annotations:
    description: "Backend Service"

spec:
  type: NodePort
  selector:
    app: express-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30001
