apiVersion: apps/v1
kind: Deployment
metadata:
name: express-backend
spec:
replicas: 2

selector:
matchLabels:
app: express-backend

template:
metadata:
labels:
app: express-backend

```
spec:
  imagePullSecrets:
    - name: ecr-secret

  containers:
    - name: express-backend
      image: 283372067875.dkr.ecr.us-east-1.amazonaws.com/express-backend:latest
      imagePullPolicy: Always

      ports:
        - containerPort: 5000


